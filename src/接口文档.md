## CSC4001项目 接口文档 
(remeber to read cookie "studentID" after the user login or creates an account)
<br />

#### 1. Sign In
    URL:        "/signin"
    Method:     "POST"
    Data Form:  JSON
      {
        studentID:   string,
        password:   string,
      }
    Return:     JSON
      {
        exist:      boolean,
        //  stuID:      number,    (null when exist = false)
        correctPwd: boolean
      }
##### Remark: 
  &emsp; stuID是我们用来唯一标识一个学生的数字（可以是1，2，3，...）
  &emsp; studentID是学校现有的学生号（如：118010200）
  
#### 1.5 search stu

    URL:        "/searchStu/<string:stuid>"
    Method:     "GET"
    Return:     JSON
      {
        exist:      boolean,
      }
<br />

#### 2. Sign Up
##### changed: now college is available
    URL:        "/signup",
    Method:     "POST",
    Data Form:  JSON
      {
        userName:   string,
        password:   string,
        name:       string,
        studentID:  string,
        school:     string,
        college:    string,
        major:      string,
      }
    Return:     JSON
      {
        studentID:    string,
        crated:       bool,
        error:        str
        
      }
<br />



#### 3. Get Student Info
##### changed: now college and gender is available
    URL:        "/getStudentInfo/[string:studentID]",
    Method:     "GET",
    Return:     JSON
      {
        name:       string,
        studentID:  string,
        year:       number,
        gender:     boolean,
        school:     string,
        college:    string,
        major:      string,
        tot_credit: int,
        // weeklySchedule: [
        //   {
            
        //   },
        //  ...
        //  ]
      }

##### Remark: Weekly Schedule格式
    {
      confirmed: [
        data format of session
      ],
      addedNotConfirmed: [      // 也可以改名为added
        data format of session
      ],
      toAdd: (!!! Optional !!!) [
        data format of session
      ],
    }
各阶段解释：
&emsp;  confirmed: 已经确认抢到的课程
&emsp;  addedNotConfirmed: 已添加到购物车，但还没有最终确定的课程
&emsp;  toAdd: 即将添加到购物车的课程，前端会进行操作（!!! 后端可以不要这个 !!!）
<br />


#### 4. Get Term Info (查询所有学期的列表)
    URL:        "/getTermInfo",
    Method:     "GET",
    Return:     JSON
      [
        "2018-2019 Term 1",
        "2018-2019 Term 2",
        "2018-2019 Summer Term",
        ...
      ]
<br />

#### 5. Search Course
    URL:        "/searchCourse",
    Method:     "POST",
    Data Form:   JSON
      {
        coursePrefix:   string [Example: "CSC"],
        courseCode:     number [Example: 1001],
        school:         string [Example: SSE],
        targetStudent:  string [Example: Undergraduate],
      }
    Return:     JSON    (return a list of course infos)
      [
        {
          data about a course
        },
        ...
      ]

      
##### 5.1: Data Format of a Course
    {
      title:    string [Example: CSC1001],
      fullName: string [Example: CSC1001 - Introduction to Computational Programming,
      code:     number [Example: 1001]
      credit:   number [Example: 3],
      school:   string [Example: SSE],
      term:     string [Example: "2018-2019 Term 1"],
      mode:     string [Exmaple: "onsite"],
      targetStudent:    string [Example: "Undergraduate"],
      introduction:     string [Example: a long string of course introduction],
      markingCriteria: [
        {
          item:   xxx [Example: Assignments],
          weight: xxx [Example: "10%"],
        },
        ...
      ],
      syllabus: URLstring [Example: a link to a file refering to the course syllabus],
      prerequisite: [
        "CSC1001",
        ...
      ],
      session: [
        {
          data format for a session
        },
        ...
      ]
    }

##### 5.2: Data Format for session
###### changed: courseTitile added
    {
      (!!! 可以添加courseTitle以便更好地检索session !!!)
      sessionNumber:      number [Example: 1],
      courseTitle:        string [Exp: CSC1001]
      isLecture:          boolean [Example: true],      标识这个session是Lecture还是Tutorial
      instructor:         string  [Example: "Rui Huang"],
      timeSlots: [
        {
          weekday:            string  [Example: "Monday"],
          beginTime:          string  [Example: "13:00 - 14:20"],
          endTime:            string  [Example: "14:30 - 15:50"],
        },
        ...
      ]
      location:           string  [Example: "TB 105"],
      currentEnrollment:  number  [Example: 138],
      classCapacity:      number  [Example: 150],
    }
<br />

#### 6. Create Course Instance
##### when click into specific course page?
    URL:        "/coursePage/<string:courseTitle>",
    Method:     "GET",
    Return:     JSON    
      {
        'create'： boolean [True:success]
      }
      
#### 6.1 Get instructor info
##### this info maybe used when click into specific course page. Lec instrs only.
    URL:        "/getInstr/<string:courseTitle>",
    Method:     "GET",
    Return:     JSON    
      {
        { 
            name:     str
            school:   str
            website:  str
            profile:  str
        }
        ....
      }



#### 7. add class to confirmed list (对weekly schedule进行操作)
    URL:          /addClass,
    method:       POST,
    Data Format:  JSON
      [
        {
          data format of a session
        },
        ...
      ]
Remark: 可能会同时加多门课到confirmed当中，需要先判断这些课是否都在addedNotConfirmed中。
<br />

#### 8. test if session is conflict
    URL:          /isSessionConflict,
    method:       POST,
    Data Format:  JSON
      [
        {
          data format of a session
        }
      ]
    Return:       JSON
      {
        conflict:   boolean,
      }
<br />

#### 9. delete an added course session
    URL:          /removeAddedCourse,
    method:       POST,
    Data Format:  JSON
      [
        {
          data format of a session
        },
        ...
      ]
    Return:       JSON
      {
        success:    boolean,
      }
<br />

#### 10.1 拉取Comment（课程评价）的接口

    URL:          /getCourseComment,
    method:       POST,
    Data Format:  JSON

       {
          courseTitle:   str  [e.g. CSC1001]
       },

    Return:       JSON
      {
        avgRating:    float
        comments:  [
            {
                comment data format
            }
        ]
      }
<br />

#### 10.2 增加Comment（课程评价）的接口

    URL:          /postCourseComment,
    method:       POST,
    Data Format:  JSON
       {
          courseTitle:   str  [e.g. CSC1001]
          studentID:  string, [118]
          author:     string, [name]
          rating:     int,    [0-5]
          content:    string,
       },

    Return:       JSON
      {
           succeed:  bool
      }
<br />

##### 课程评价数据格式
###### edited: add rating
    {
      commentID:  number,
      author:     string,
      datetime:   string,   [Example: "2021-05-05 23:00PM"]
      rating:     int
      content:    string,
      comments: [] (array of commentID)     （!!! 一个评价可以有子评论，不过这个不着急实现 !!!）
    }
<br />

#### Auto Mode 相关的接口你们先根据后端自己设计
